import { FavoriteButtonDirective } from './favorite-button.directive';

describe('FavoriteButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoriteButtonDirective();
    expect(directive).toBeTruthy();
  });
});
