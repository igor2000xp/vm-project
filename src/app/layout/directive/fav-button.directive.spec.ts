import { FavButtonDirective } from './fav-button.directive';

describe('FavButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FavButtonDirective();
    expect(directive).toBeTruthy();
  });
});
